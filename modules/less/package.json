{
  "name": "rollup-plugin-less",
  "version": "0.0.0",
  "main": "dist/plugin.js",
  "module": "lib/plugin.js",
  "license": "MIT",
  "scripts": {
    "test": "./run-test",
    "clean": "rm -rf dist",
    "transpile": "rollup -c",
    "prepublishOnly": "yarn clean && yarn transpile"
  },
  "dependencies": {
    "fs-extra": "^9.1.0",
    "less": "^4.1.1"
  },
  "devDependencies": {
    "rollup": "^2.41.0"
  }
}
