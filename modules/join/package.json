{
  "name": "rollup-plugin-join",
  "version": "0.0.0",
  "main": "dist/plugin.js",
  "license": "MIT",
  "scripts": {
    "test": "./run-test",
    "clean": "rm -rf dist",
    "transpile": "rollup -c",
    "prepublishOnly": "yarn clean && yarn transpile"
  },
  "dependencies": {
    "fs-extra": "^9.1.0"
  },
  "devDependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "rollup": "^2.41.0"
  }
}
